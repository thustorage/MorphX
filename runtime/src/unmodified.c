// auto-generated by parse.py, used with modified.c

CUresult cuGetErrorString(CUresult error, const char** pStr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGetErrorString(error, pStr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGetErrorString %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGetErrorName(CUresult error, const char** pStr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGetErrorName(error, pStr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGetErrorName %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuInit(unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuInit(Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuInit %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDriverGetVersion(int* driverVersion) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDriverGetVersion(driverVersion); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDriverGetVersion %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGet(CUdevice* device, int ordinal) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGet(device, ordinal); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGet %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetCount(int* count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetCount(count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetCount %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetName(char* name, int len, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetName(name, len, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetName %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetUuid(CUuuid* uuid, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetUuid(uuid, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetUuid %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetUuid_v2(CUuuid* uuid, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetUuid_v2(uuid, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetUuid_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetLuid(char* luid, unsigned int* deviceNodeMask, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetLuid(luid, deviceNodeMask, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetLuid %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceTotalMem(size_t* bytes, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceTotalMem(bytes, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceTotalMem %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetTexture1DLinearMaxWidth(size_t* maxWidthInElements, CUarray_format format, unsigned numChannels, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetTexture1DLinearMaxWidth(maxWidthInElements, format, numChannels, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetTexture1DLinearMaxWidth %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetAttribute(int* pi, CUdevice_attribute attrib, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetAttribute(pi, attrib, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetNvSciSyncAttributes(void* nvSciSyncAttrList, CUdevice dev, int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetNvSciSyncAttributes(nvSciSyncAttrList, dev, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetNvSciSyncAttributes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceSetMemPool(CUdevice dev, CUmemoryPool pool) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceSetMemPool(dev, pool); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceSetMemPool %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetMemPool(CUmemoryPool* pool, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetMemPool(pool, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetMemPool %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetDefaultMemPool(CUmemoryPool* pool_out, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetDefaultMemPool(pool_out, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetDefaultMemPool %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetExecAffinitySupport(int* pi, CUexecAffinityType type, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetExecAffinitySupport(pi, type, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetExecAffinitySupport %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFlushGPUDirectRDMAWrites(CUflushGPUDirectRDMAWritesTarget target, CUflushGPUDirectRDMAWritesScope scope) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFlushGPUDirectRDMAWrites(target, scope); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFlushGPUDirectRDMAWrites %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetProperties(CUdevprop* prop, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetProperties(prop, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetProperties %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceComputeCapability(int* major, int* minor, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceComputeCapability(major, minor, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceComputeCapability %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxRetain(CUcontext* pctx, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxRetain(pctx, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxRetain %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxRelease(CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxRelease(dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxRelease %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxSetFlags(CUdevice dev, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxSetFlags(dev, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxSetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxGetState(CUdevice dev, unsigned int* flags, int* active) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxGetState(dev, flags, active); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxGetState %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxReset(CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxReset(dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxReset %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxCreate(CUcontext* pctx, unsigned int flags, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxCreate(pctx, flags, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxCreate_v3(CUcontext* pctx, CUexecAffinityParam* paramsArray, int numParams, unsigned int flags, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxCreate_v3(pctx, paramsArray, numParams, flags, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxCreate_v3 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxDestroy(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxDestroy(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxPushCurrent(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxPushCurrent(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxPushCurrent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxPopCurrent(CUcontext* pctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxPopCurrent(pctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxPopCurrent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSetCurrent(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSetCurrent(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSetCurrent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetCurrent(CUcontext* pctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetCurrent(pctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetCurrent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetDevice(CUdevice* device) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetDevice(device); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetDevice %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetFlags(unsigned int* flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetFlags(flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSetFlags(unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSetFlags(flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetId(CUcontext ctx, unsigned long long* ctxId) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetId(ctx, ctxId); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetId %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSynchronize(void ) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSynchronize(); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSynchronize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSetLimit(CUlimit limit, size_t value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSetLimit(limit, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSetLimit %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetLimit(size_t* pvalue, CUlimit limit) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetLimit(pvalue, limit); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetLimit %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetCacheConfig(CUfunc_cache* pconfig) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetCacheConfig(pconfig); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetCacheConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSetCacheConfig(CUfunc_cache config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSetCacheConfig(config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSetCacheConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetApiVersion(CUcontext ctx, unsigned int* version) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetApiVersion(ctx, version); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetApiVersion %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetStreamPriorityRange(int* leastPriority, int* greatestPriority) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetStreamPriorityRange(leastPriority, greatestPriority); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetStreamPriorityRange %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxResetPersistingL2Cache(void ) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxResetPersistingL2Cache(); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxResetPersistingL2Cache %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetExecAffinity(CUexecAffinityParam* pExecAffinity, CUexecAffinityType type) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetExecAffinity(pExecAffinity, type); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetExecAffinity %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxAttach(CUcontext* pctx, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxAttach(pctx, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxAttach %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxDetach(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxDetach(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxDetach %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetSharedMemConfig(CUsharedconfig* pConfig) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetSharedMemConfig(pConfig); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetSharedMemConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxSetSharedMemConfig(CUsharedconfig config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxSetSharedMemConfig(config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxSetSharedMemConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleUnload(CUmodule hmod) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleUnload(hmod); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleUnload %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetLoadingMode(CUmoduleLoadingMode* mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetLoadingMode(mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetLoadingMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetFunctionCount(unsigned int* count, CUmodule mod) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetFunctionCount(count, mod); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetFunctionCount %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleEnumerateFunctions(CUfunction* functions, unsigned int numFunctions, CUmodule mod) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleEnumerateFunctions(functions, numFunctions, mod); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleEnumerateFunctions %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetGlobal(CUdeviceptr* dptr, size_t* bytes, CUmodule hmod, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetGlobal(dptr, bytes, hmod, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetGlobal %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkCreate(unsigned int numOptions, CUjit_option* options, void** optionValues, CUlinkState* stateOut) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkCreate(numOptions, options, optionValues, stateOut); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkAddData(CUlinkState state, CUjitInputType type, void* data, size_t size, const char* name, unsigned int numOptions, CUjit_option* options, void** optionValues) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkAddData(state, type, data, size, name, numOptions, options, optionValues); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkAddData %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkAddFile(CUlinkState state, CUjitInputType type, const char* path, unsigned int numOptions, CUjit_option* options, void** optionValues) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkAddFile(state, type, path, numOptions, options, optionValues); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkAddFile %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkComplete(CUlinkState state, void** cubinOut, size_t* sizeOut) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkComplete(state, cubinOut, sizeOut); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkComplete %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkDestroy(CUlinkState state) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkDestroy(state); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetTexRef(CUtexref* pTexRef, CUmodule hmod, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetTexRef(pTexRef, hmod, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetTexRef %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetSurfRef(CUsurfref* pSurfRef, CUmodule hmod, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetSurfRef(pSurfRef, hmod, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetSurfRef %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryLoadFromFile(CUlibrary* library, const char* fileName, CUjit_option* jitOptions, void** jitOptionsValues, unsigned int numJitOptions, CUlibraryOption* libraryOptions, void** libraryOptionValues, unsigned int numLibraryOptions) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryLoadFromFile(library, fileName, jitOptions, jitOptionsValues, numJitOptions, libraryOptions, libraryOptionValues, numLibraryOptions); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryLoadFromFile %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryUnload(CUlibrary library) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryUnload(library); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryUnload %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryGetKernelCount(unsigned int* count, CUlibrary lib) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryGetKernelCount(count, lib); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryGetKernelCount %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryEnumerateKernels(CUkernel* kernels, unsigned int numKernels, CUlibrary lib) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryEnumerateKernels(kernels, numKernels, lib); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryEnumerateKernels %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryGetGlobal(CUdeviceptr* dptr, size_t* bytes, CUlibrary library, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryGetGlobal(dptr, bytes, library, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryGetGlobal %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryGetManaged(CUdeviceptr* dptr, size_t* bytes, CUlibrary library, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryGetManaged(dptr, bytes, library, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryGetManaged %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLibraryGetUnifiedFunction(void** fptr, CUlibrary library, const char* symbol) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLibraryGetUnifiedFunction(fptr, library, symbol); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLibraryGetUnifiedFunction %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuKernelGetAttribute(int* pi, CUfunction_attribute attrib, CUkernel kernel, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuKernelGetAttribute(pi, attrib, kernel, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuKernelGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuKernelSetAttribute(CUfunction_attribute attrib, int val, CUkernel kernel, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuKernelSetAttribute(attrib, val, kernel, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuKernelSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuKernelSetCacheConfig(CUkernel kernel, CUfunc_cache config, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuKernelSetCacheConfig(kernel, config, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuKernelSetCacheConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuKernelGetName(const char** name, CUkernel hfunc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuKernelGetName(name, hfunc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuKernelGetName %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuKernelGetParamInfo(CUkernel kernel, size_t paramIndex, size_t* paramOffset, size_t* paramSize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuKernelGetParamInfo(kernel, paramIndex, paramOffset, paramSize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuKernelGetParamInfo %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetInfo(size_t* free, size_t* total) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetInfo(free, total); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetInfo %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAlloc(CUdeviceptr* dptr, size_t bytesize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAlloc(dptr, bytesize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAlloc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocPitch(CUdeviceptr* dptr, size_t* pPitch, size_t WidthInBytes, size_t Height, unsigned int ElementSizeBytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocPitch(dptr, pPitch, WidthInBytes, Height, ElementSizeBytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocPitch %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemFree(CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemFree(dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemFree %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetAddressRange(CUdeviceptr* pbase, size_t* psize, CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetAddressRange(pbase, psize, dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetAddressRange %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocHost(void** pp, size_t bytesize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocHost(pp, bytesize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocHost %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemFreeHost(void* p) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemFreeHost(p); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemFreeHost %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostAlloc(void** pp, size_t bytesize, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostAlloc(pp, bytesize, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostAlloc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostGetDevicePointer(CUdeviceptr* pdptr, void* p, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostGetDevicePointer(pdptr, p, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostGetDevicePointer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostGetFlags(unsigned int* pFlags, void* p) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostGetFlags(pFlags, p); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostGetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocManaged(CUdeviceptr* dptr, size_t bytesize, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocManaged(dptr, bytesize, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocManaged %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceRegisterAsyncNotification(CUdevice device, CUasyncCallback callbackFunc, void* userData, CUasyncCallbackHandle* callback) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceRegisterAsyncNotification(device, callbackFunc, userData, callback); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceRegisterAsyncNotification %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceUnregisterAsyncNotification(CUdevice device, CUasyncCallbackHandle callback) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceUnregisterAsyncNotification(device, callback); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceUnregisterAsyncNotification %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetByPCIBusId(CUdevice* dev, const char* pciBusId) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetByPCIBusId(dev, pciBusId); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetByPCIBusId %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetPCIBusId(char* pciBusId, int len, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetPCIBusId(pciBusId, len, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetPCIBusId %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcGetEventHandle(CUipcEventHandle* pHandle, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcGetEventHandle(pHandle, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcGetEventHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcOpenEventHandle(CUevent* phEvent, CUipcEventHandle handle) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcOpenEventHandle(phEvent, handle); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcOpenEventHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcGetMemHandle(CUipcMemHandle* pHandle, CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcGetMemHandle(pHandle, dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcGetMemHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcOpenMemHandle(CUdeviceptr* pdptr, CUipcMemHandle handle, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcOpenMemHandle(pdptr, handle, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcOpenMemHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcCloseMemHandle(CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcCloseMemHandle(dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcCloseMemHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostRegister(void* p, size_t bytesize, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostRegister(p, bytesize, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostRegister %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostUnregister(void* p) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostUnregister(p); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostUnregister %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy(CUdeviceptr dst, CUdeviceptr src, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy(dst, src, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyPeer(CUdeviceptr dstDevice, CUcontext dstContext, CUdeviceptr srcDevice, CUcontext srcContext, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyPeer(dstDevice, dstContext, srcDevice, srcContext, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyPeer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoD(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoD(dstDevice, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoD %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoH(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoH(dstHost, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoH %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoD(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoD(dstDevice, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoD %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoA(CUarray dstArray, size_t dstOffset, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoA(dstArray, dstOffset, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoA %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoD(CUdeviceptr dstDevice, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoD(dstDevice, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoD %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoA(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoA(dstArray, dstOffset, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoA %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoH(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoH(dstHost, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoH %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoA(CUarray dstArray, size_t dstOffset, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoA(dstArray, dstOffset, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoA %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2D(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2D(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2D %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DUnaligned(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DUnaligned(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DUnaligned %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3D(const CUDA_MEMCPY3D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3D(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3D %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DPeer(const CUDA_MEMCPY3D_PEER* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DPeer(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DPeer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAsync(CUdeviceptr dst, CUdeviceptr src, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAsync(dst, src, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyPeerAsync(CUdeviceptr dstDevice, CUcontext dstContext, CUdeviceptr srcDevice, CUcontext srcContext, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyPeerAsync(dstDevice, dstContext, srcDevice, srcContext, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyPeerAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoDAsync(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoDAsync(dstDevice, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoDAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoHAsync(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoHAsync(dstHost, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoHAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoDAsync(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoDAsync(dstDevice, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoDAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoAAsync(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoAAsync(dstArray, dstOffset, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoAAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoHAsync(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoHAsync(dstHost, srcArray, srcOffset, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoHAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DAsync(const CUDA_MEMCPY2D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DAsync(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DAsync(const CUDA_MEMCPY3D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DAsync(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DPeerAsync(const CUDA_MEMCPY3D_PEER* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DPeerAsync(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DPeerAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD8(CUdeviceptr dstDevice, unsigned char uc, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD8(dstDevice, uc, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD8 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD16(CUdeviceptr dstDevice, unsigned short us, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD16(dstDevice, us, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD16 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD32(CUdeviceptr dstDevice, unsigned int ui, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD32(dstDevice, ui, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD32 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D8(CUdeviceptr dstDevice, size_t dstPitch, unsigned char uc, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D8(dstDevice, dstPitch, uc, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D8 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D16(CUdeviceptr dstDevice, size_t dstPitch, unsigned short us, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D16(dstDevice, dstPitch, us, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D16 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D32(CUdeviceptr dstDevice, size_t dstPitch, unsigned int ui, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D32(dstDevice, dstPitch, ui, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D32 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD8Async(CUdeviceptr dstDevice, unsigned char uc, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD8Async(dstDevice, uc, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD8Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD16Async(CUdeviceptr dstDevice, unsigned short us, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD16Async(dstDevice, us, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD16Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD32Async(CUdeviceptr dstDevice, unsigned int ui, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD32Async(dstDevice, ui, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD32Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D8Async(CUdeviceptr dstDevice, size_t dstPitch, unsigned char uc, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D8Async(dstDevice, dstPitch, uc, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D8Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D16Async(CUdeviceptr dstDevice, size_t dstPitch, unsigned short us, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D16Async(dstDevice, dstPitch, us, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D16Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D32Async(CUdeviceptr dstDevice, size_t dstPitch, unsigned int ui, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D32Async(dstDevice, dstPitch, ui, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D32Async %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayCreate(CUarray* pHandle, const CUDA_ARRAY_DESCRIPTOR* pAllocateArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayCreate(pHandle, pAllocateArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayGetDescriptor(CUDA_ARRAY_DESCRIPTOR* pArrayDescriptor, CUarray hArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayGetDescriptor(pArrayDescriptor, hArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayGetDescriptor %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayGetSparseProperties(CUDA_ARRAY_SPARSE_PROPERTIES* sparseProperties, CUarray array) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayGetSparseProperties(sparseProperties, array); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayGetSparseProperties %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMipmappedArrayGetSparseProperties(CUDA_ARRAY_SPARSE_PROPERTIES* sparseProperties, CUmipmappedArray mipmap) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMipmappedArrayGetSparseProperties(sparseProperties, mipmap); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMipmappedArrayGetSparseProperties %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayGetMemoryRequirements(CUDA_ARRAY_MEMORY_REQUIREMENTS* memoryRequirements, CUarray array, CUdevice device) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayGetMemoryRequirements(memoryRequirements, array, device); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayGetMemoryRequirements %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMipmappedArrayGetMemoryRequirements(CUDA_ARRAY_MEMORY_REQUIREMENTS* memoryRequirements, CUmipmappedArray mipmap, CUdevice device) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMipmappedArrayGetMemoryRequirements(memoryRequirements, mipmap, device); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMipmappedArrayGetMemoryRequirements %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayGetPlane(CUarray* pPlaneArray, CUarray hArray, unsigned int planeIdx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayGetPlane(pPlaneArray, hArray, planeIdx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayGetPlane %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayDestroy(CUarray hArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayDestroy(hArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArray3DCreate(CUarray* pHandle, const CUDA_ARRAY3D_DESCRIPTOR* pAllocateArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArray3DCreate(pHandle, pAllocateArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArray3DCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArray3DGetDescriptor(CUDA_ARRAY3D_DESCRIPTOR* pArrayDescriptor, CUarray hArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArray3DGetDescriptor(pArrayDescriptor, hArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArray3DGetDescriptor %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMipmappedArrayCreate(CUmipmappedArray* pHandle, const CUDA_ARRAY3D_DESCRIPTOR* pMipmappedArrayDesc, unsigned int numMipmapLevels) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMipmappedArrayCreate(pHandle, pMipmappedArrayDesc, numMipmapLevels); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMipmappedArrayCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMipmappedArrayGetLevel(CUarray* pLevelArray, CUmipmappedArray hMipmappedArray, unsigned int level) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMipmappedArrayGetLevel(pLevelArray, hMipmappedArray, level); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMipmappedArrayGetLevel %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMipmappedArrayDestroy(CUmipmappedArray hMipmappedArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMipmappedArrayDestroy(hMipmappedArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMipmappedArrayDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetHandleForAddressRange(void* handle, CUdeviceptr dptr, size_t size, CUmemRangeHandleType handleType, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetHandleForAddressRange(handle, dptr, size, handleType, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetHandleForAddressRange %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAddressReserve(CUdeviceptr* ptr, size_t size, size_t alignment, CUdeviceptr addr, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAddressReserve(ptr, size, alignment, addr, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAddressReserve %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAddressFree(CUdeviceptr ptr, size_t size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAddressFree(ptr, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAddressFree %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemCreate(CUmemGenericAllocationHandle* handle, size_t size, const CUmemAllocationProp* prop, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemCreate(handle, size, prop, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemRelease(CUmemGenericAllocationHandle handle) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemRelease(handle); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemRelease %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemMap(CUdeviceptr ptr, size_t size, size_t offset, CUmemGenericAllocationHandle handle, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemMap(ptr, size, offset, handle, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemMap %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemMapArrayAsync(CUarrayMapInfo* mapInfoList, unsigned int count, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemMapArrayAsync(mapInfoList, count, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemMapArrayAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemUnmap(CUdeviceptr ptr, size_t size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemUnmap(ptr, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemUnmap %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemSetAccess(CUdeviceptr ptr, size_t size, const CUmemAccessDesc* desc, size_t count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemSetAccess(ptr, size, desc, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemSetAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetAccess(unsigned long long* flags, const CUmemLocation* location, CUdeviceptr ptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetAccess(flags, location, ptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemExportToShareableHandle(void* shareableHandle, CUmemGenericAllocationHandle handle, CUmemAllocationHandleType handleType, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemExportToShareableHandle(shareableHandle, handle, handleType, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemExportToShareableHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemImportFromShareableHandle(CUmemGenericAllocationHandle* handle, void* osHandle, CUmemAllocationHandleType shHandleType) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemImportFromShareableHandle(handle, osHandle, shHandleType); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemImportFromShareableHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetAllocationGranularity(size_t* granularity, const CUmemAllocationProp* prop, CUmemAllocationGranularity_flags option) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetAllocationGranularity(granularity, prop, option); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetAllocationGranularity %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetAllocationPropertiesFromHandle(CUmemAllocationProp* prop, CUmemGenericAllocationHandle handle) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetAllocationPropertiesFromHandle(prop, handle); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetAllocationPropertiesFromHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemRetainAllocationHandle(CUmemGenericAllocationHandle* handle, void* addr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemRetainAllocationHandle(handle, addr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemRetainAllocationHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemFreeAsync(CUdeviceptr dptr, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemFreeAsync(dptr, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemFreeAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocAsync(CUdeviceptr* dptr, size_t bytesize, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocAsync(dptr, bytesize, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolTrimTo(CUmemoryPool pool, size_t minBytesToKeep) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolTrimTo(pool, minBytesToKeep); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolTrimTo %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolSetAttribute(CUmemoryPool pool, CUmemPool_attribute attr, void* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolSetAttribute(pool, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolGetAttribute(CUmemoryPool pool, CUmemPool_attribute attr, void* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolGetAttribute(pool, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolSetAccess(CUmemoryPool pool, const CUmemAccessDesc* map, size_t count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolSetAccess(pool, map, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolSetAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolGetAccess(CUmemAccess_flags* flags, CUmemoryPool memPool, CUmemLocation* location) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolGetAccess(flags, memPool, location); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolGetAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolCreate(CUmemoryPool* pool, const CUmemPoolProps* poolProps) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolCreate(pool, poolProps); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolDestroy(CUmemoryPool pool) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolDestroy(pool); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocFromPoolAsync(CUdeviceptr* dptr, size_t bytesize, CUmemoryPool pool, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocFromPoolAsync(dptr, bytesize, pool, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocFromPoolAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolExportToShareableHandle(void* handle_out, CUmemoryPool pool, CUmemAllocationHandleType handleType, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolExportToShareableHandle(handle_out, pool, handleType, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolExportToShareableHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolImportFromShareableHandle(CUmemoryPool* pool_out, void* handle, CUmemAllocationHandleType handleType, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolImportFromShareableHandle(pool_out, handle, handleType, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolImportFromShareableHandle %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolExportPointer(CUmemPoolPtrExportData* shareData_out, CUdeviceptr ptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolExportPointer(shareData_out, ptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolExportPointer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPoolImportPointer(CUdeviceptr* ptr_out, CUmemoryPool pool, CUmemPoolPtrExportData* shareData) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPoolImportPointer(ptr_out, pool, shareData); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPoolImportPointer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastCreate(CUmemGenericAllocationHandle* mcHandle, const CUmulticastObjectProp* prop) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastCreate(mcHandle, prop); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastAddDevice(CUmemGenericAllocationHandle mcHandle, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastAddDevice(mcHandle, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastAddDevice %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastBindMem(CUmemGenericAllocationHandle mcHandle, size_t mcOffset, CUmemGenericAllocationHandle memHandle, size_t memOffset, size_t size, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastBindMem(mcHandle, mcOffset, memHandle, memOffset, size, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastBindMem %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastBindAddr(CUmemGenericAllocationHandle mcHandle, size_t mcOffset, CUdeviceptr memptr, size_t size, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastBindAddr(mcHandle, mcOffset, memptr, size, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastBindAddr %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastUnbind(CUmemGenericAllocationHandle mcHandle, CUdevice dev, size_t mcOffset, size_t size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastUnbind(mcHandle, dev, mcOffset, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastUnbind %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMulticastGetGranularity(size_t* granularity, const CUmulticastObjectProp* prop, CUmulticastGranularity_flags option) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMulticastGetGranularity(granularity, prop, option); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMulticastGetGranularity %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuPointerGetAttribute(void* data, CUpointer_attribute attribute, CUdeviceptr ptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuPointerGetAttribute(data, attribute, ptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuPointerGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPrefetchAsync(CUdeviceptr devPtr, size_t count, CUdevice dstDevice, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPrefetchAsync(devPtr, count, dstDevice, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPrefetchAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPrefetchAsync_v2(CUdeviceptr devPtr, size_t count, CUmemLocation location, unsigned int flags, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPrefetchAsync_v2(devPtr, count, location, flags, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPrefetchAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAdvise(CUdeviceptr devPtr, size_t count, CUmem_advise advice, CUdevice device) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAdvise(devPtr, count, advice, device); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAdvise %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAdvise_v2(CUdeviceptr devPtr, size_t count, CUmem_advise advice, CUmemLocation location) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAdvise_v2(devPtr, count, advice, location); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAdvise_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemRangeGetAttribute(void* data, size_t dataSize, CUmem_range_attribute attribute, CUdeviceptr devPtr, size_t count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemRangeGetAttribute(data, dataSize, attribute, devPtr, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemRangeGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemRangeGetAttributes(void** data, size_t* dataSizes, CUmem_range_attribute* attributes, size_t numAttributes, CUdeviceptr devPtr, size_t count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemRangeGetAttributes(data, dataSizes, attributes, numAttributes, devPtr, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemRangeGetAttributes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuPointerSetAttribute(const void* value, CUpointer_attribute attribute, CUdeviceptr ptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuPointerSetAttribute(value, attribute, ptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuPointerSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuPointerGetAttributes(unsigned int numAttributes, CUpointer_attribute* attributes, void** data, CUdeviceptr ptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuPointerGetAttributes(numAttributes, attributes, data, ptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuPointerGetAttributes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamCreate(CUstream* phStream, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamCreate(phStream, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamCreateWithPriority(CUstream* phStream, unsigned int flags, int priority) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamCreateWithPriority(phStream, flags, priority); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamCreateWithPriority %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetPriority(CUstream hStream, int* priority) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetPriority(hStream, priority); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetPriority %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetFlags(CUstream hStream, unsigned int* flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetFlags(hStream, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetId(CUstream hStream, unsigned long long* streamId) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetId(hStream, streamId); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetId %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCtx(CUstream hStream, CUcontext* pctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCtx(hStream, pctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCtx %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitEvent(CUstream hStream, CUevent hEvent, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitEvent(hStream, hEvent, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamAddCallback(CUstream hStream, CUstreamCallback callback, void* userData, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamAddCallback(hStream, callback, userData, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamAddCallback %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCapture(CUstream hStream, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCapture(hStream, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCapture %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCaptureToGraph(CUstream hStream, CUgraph hGraph, const CUgraphNode* dependencies, const CUgraphEdgeData* dependencyData, size_t numDependencies, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCaptureToGraph(hStream, hGraph, dependencies, dependencyData, numDependencies, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCaptureToGraph %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuThreadExchangeStreamCaptureMode(CUstreamCaptureMode* mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuThreadExchangeStreamCaptureMode(mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuThreadExchangeStreamCaptureMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamEndCapture(CUstream hStream, CUgraph* phGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamEndCapture(hStream, phGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamEndCapture %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamIsCapturing(CUstream hStream, CUstreamCaptureStatus* captureStatus) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamIsCapturing(hStream, captureStatus); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamIsCapturing %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo(hStream, captureStatus_out, id_out, graph_out, dependencies_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo_v3(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, const CUgraphEdgeData** edgeData_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo_v3(hStream, captureStatus_out, id_out, graph_out, dependencies_out, edgeData_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo_v3 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamUpdateCaptureDependencies(CUstream hStream, CUgraphNode* dependencies, size_t numDependencies, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamUpdateCaptureDependencies(hStream, dependencies, numDependencies, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamUpdateCaptureDependencies %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamUpdateCaptureDependencies_v2(CUstream hStream, CUgraphNode* dependencies, const CUgraphEdgeData* dependencyData, size_t numDependencies, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamUpdateCaptureDependencies_v2(hStream, dependencies, dependencyData, numDependencies, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamUpdateCaptureDependencies_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamAttachMemAsync(CUstream hStream, CUdeviceptr dptr, size_t length, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamAttachMemAsync(hStream, dptr, length, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamAttachMemAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamQuery(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamQuery(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamQuery %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamSynchronize(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamSynchronize(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamSynchronize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamDestroy(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamDestroy(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamCopyAttributes(CUstream dst, CUstream src) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamCopyAttributes(dst, src); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamCopyAttributes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetAttribute(CUstream hStream, CUstreamAttrID attr, CUstreamAttrValue* value_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetAttribute(hStream, attr, value_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamSetAttribute(CUstream hStream, CUstreamAttrID attr, const CUstreamAttrValue* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamSetAttribute(hStream, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventCreate(CUevent* phEvent, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventCreate(phEvent, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventRecord(CUevent hEvent, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventRecord(hEvent, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventRecord %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventRecordWithFlags(CUevent hEvent, CUstream hStream, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventRecordWithFlags(hEvent, hStream, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventRecordWithFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventQuery(CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventQuery(hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventQuery %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventSynchronize(CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventSynchronize(hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventSynchronize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventDestroy(CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventDestroy(hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventElapsedTime(float* pMilliseconds, CUevent hStart, CUevent hEnd) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventElapsedTime(pMilliseconds, hStart, hEnd); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventElapsedTime %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuImportExternalMemory(CUexternalMemory* extMem_out, const CUDA_EXTERNAL_MEMORY_HANDLE_DESC* memHandleDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuImportExternalMemory(extMem_out, memHandleDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuImportExternalMemory %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuExternalMemoryGetMappedBuffer(CUdeviceptr* devPtr, CUexternalMemory extMem, const CUDA_EXTERNAL_MEMORY_BUFFER_DESC* bufferDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuExternalMemoryGetMappedBuffer(devPtr, extMem, bufferDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuExternalMemoryGetMappedBuffer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuExternalMemoryGetMappedMipmappedArray(CUmipmappedArray* mipmap, CUexternalMemory extMem, const CUDA_EXTERNAL_MEMORY_MIPMAPPED_ARRAY_DESC* mipmapDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuExternalMemoryGetMappedMipmappedArray(mipmap, extMem, mipmapDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuExternalMemoryGetMappedMipmappedArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDestroyExternalMemory(CUexternalMemory extMem) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDestroyExternalMemory(extMem); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDestroyExternalMemory %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuImportExternalSemaphore(CUexternalSemaphore* extSem_out, const CUDA_EXTERNAL_SEMAPHORE_HANDLE_DESC* semHandleDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuImportExternalSemaphore(extSem_out, semHandleDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuImportExternalSemaphore %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSignalExternalSemaphoresAsync(const CUexternalSemaphore* extSemArray, const CUDA_EXTERNAL_SEMAPHORE_SIGNAL_PARAMS* paramsArray, unsigned int numExtSems, CUstream stream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSignalExternalSemaphoresAsync(extSemArray, paramsArray, numExtSems, stream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSignalExternalSemaphoresAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuWaitExternalSemaphoresAsync(const CUexternalSemaphore* extSemArray, const CUDA_EXTERNAL_SEMAPHORE_WAIT_PARAMS* paramsArray, unsigned int numExtSems, CUstream stream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuWaitExternalSemaphoresAsync(extSemArray, paramsArray, numExtSems, stream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuWaitExternalSemaphoresAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDestroyExternalSemaphore(CUexternalSemaphore extSem) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDestroyExternalSemaphore(extSem); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDestroyExternalSemaphore %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue32(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue32(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue32 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue64(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue64(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue64 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue32(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue32(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue32 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue64(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue64(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue64 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBatchMemOp(CUstream stream, unsigned int count, CUstreamBatchMemOpParams* paramArray, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBatchMemOp(stream, count, paramArray, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBatchMemOp %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncGetAttribute(int* pi, CUfunction_attribute attrib, CUfunction hfunc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncGetAttribute(pi, attrib, hfunc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncSetAttribute(CUfunction hfunc, CUfunction_attribute attrib, int value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncSetAttribute(hfunc, attrib, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncSetCacheConfig(CUfunction hfunc, CUfunc_cache config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncSetCacheConfig(hfunc, config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncSetCacheConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncGetModule(CUmodule* hmod, CUfunction hfunc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncGetModule(hmod, hfunc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncGetModule %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncGetName(const char** name, CUfunction hfunc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncGetName(name, hfunc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncGetName %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncGetParamInfo(CUfunction func, size_t paramIndex, size_t* paramOffset, size_t* paramSize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncGetParamInfo(func, paramIndex, paramOffset, paramSize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncGetParamInfo %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncIsLoaded(CUfunctionLoadingState* state, CUfunction function) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncIsLoaded(state, function); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncIsLoaded %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncLoad(CUfunction function) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncLoad(function); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncLoad %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchCooperativeKernel(CUfunction f, unsigned int gridDimX, unsigned int gridDimY, unsigned int gridDimZ, unsigned int blockDimX, unsigned int blockDimY, unsigned int blockDimZ, unsigned int sharedMemBytes, CUstream hStream, void** kernelParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchCooperativeKernel(f, gridDimX, gridDimY, gridDimZ, blockDimX, blockDimY, blockDimZ, sharedMemBytes, hStream, kernelParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchCooperativeKernel %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchCooperativeKernelMultiDevice(CUDA_LAUNCH_PARAMS* launchParamsList, unsigned int numDevices, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchCooperativeKernelMultiDevice(launchParamsList, numDevices, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchCooperativeKernelMultiDevice %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchHostFunc(CUstream hStream, CUhostFn fn, void* userData) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchHostFunc(hStream, fn, userData); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchHostFunc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncSetBlockShape(CUfunction hfunc, int x, int y, int z) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncSetBlockShape(hfunc, x, y, z); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncSetBlockShape %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncSetSharedSize(CUfunction hfunc, unsigned int bytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncSetSharedSize(hfunc, bytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncSetSharedSize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuParamSetSize(CUfunction hfunc, unsigned int numbytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuParamSetSize(hfunc, numbytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuParamSetSize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuParamSeti(CUfunction hfunc, int offset, unsigned int value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuParamSeti(hfunc, offset, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuParamSeti %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuParamSetf(CUfunction hfunc, int offset, float value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuParamSetf(hfunc, offset, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuParamSetf %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuParamSetv(CUfunction hfunc, int offset, void* ptr, unsigned int numbytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuParamSetv(hfunc, offset, ptr, numbytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuParamSetv %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunch(CUfunction f) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunch(f); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunch %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchGrid(CUfunction f, int grid_width, int grid_height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchGrid(f, grid_width, grid_height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchGrid %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchGridAsync(CUfunction f, int grid_width, int grid_height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchGridAsync(f, grid_width, grid_height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchGridAsync %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuParamSetTexRef(CUfunction hfunc, int texunit, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuParamSetTexRef(hfunc, texunit, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuParamSetTexRef %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuFuncSetSharedMemConfig(CUfunction hfunc, CUsharedconfig config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuFuncSetSharedMemConfig(hfunc, config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuFuncSetSharedMemConfig %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphCreate(CUgraph* phGraph, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphCreate(phGraph, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddKernelNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddKernelNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddKernelNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeGetParams(CUgraphNode hNode, CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeGetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeSetParams(CUgraphNode hNode, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddMemcpyNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_MEMCPY3D* copyParams, CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddMemcpyNode(phGraphNode, hGraph, dependencies, numDependencies, copyParams, ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddMemcpyNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemcpyNodeGetParams(CUgraphNode hNode, CUDA_MEMCPY3D* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemcpyNodeGetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemcpyNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemcpyNodeSetParams(CUgraphNode hNode, const CUDA_MEMCPY3D* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemcpyNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemcpyNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddMemsetNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_MEMSET_NODE_PARAMS* memsetParams, CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddMemsetNode(phGraphNode, hGraph, dependencies, numDependencies, memsetParams, ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddMemsetNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemsetNodeGetParams(CUgraphNode hNode, CUDA_MEMSET_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemsetNodeGetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemsetNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemsetNodeSetParams(CUgraphNode hNode, const CUDA_MEMSET_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemsetNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemsetNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddHostNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_HOST_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddHostNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddHostNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphHostNodeGetParams(CUgraphNode hNode, CUDA_HOST_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphHostNodeGetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphHostNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphHostNodeSetParams(CUgraphNode hNode, const CUDA_HOST_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphHostNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphHostNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddChildGraphNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUgraph childGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddChildGraphNode(phGraphNode, hGraph, dependencies, numDependencies, childGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddChildGraphNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphChildGraphNodeGetGraph(CUgraphNode hNode, CUgraph* phGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphChildGraphNodeGetGraph(hNode, phGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphChildGraphNodeGetGraph %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddEmptyNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddEmptyNode(phGraphNode, hGraph, dependencies, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddEmptyNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddEventRecordNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddEventRecordNode(phGraphNode, hGraph, dependencies, numDependencies, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddEventRecordNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphEventRecordNodeGetEvent(CUgraphNode hNode, CUevent* event_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphEventRecordNodeGetEvent(hNode, event_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphEventRecordNodeGetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphEventRecordNodeSetEvent(CUgraphNode hNode, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphEventRecordNodeSetEvent(hNode, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphEventRecordNodeSetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddEventWaitNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddEventWaitNode(phGraphNode, hGraph, dependencies, numDependencies, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddEventWaitNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphEventWaitNodeGetEvent(CUgraphNode hNode, CUevent* event_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphEventWaitNodeGetEvent(hNode, event_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphEventWaitNodeGetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphEventWaitNodeSetEvent(CUgraphNode hNode, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphEventWaitNodeSetEvent(hNode, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphEventWaitNodeSetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddExternalSemaphoresSignalNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_EXT_SEM_SIGNAL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddExternalSemaphoresSignalNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddExternalSemaphoresSignalNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExternalSemaphoresSignalNodeGetParams(CUgraphNode hNode, CUDA_EXT_SEM_SIGNAL_NODE_PARAMS* params_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExternalSemaphoresSignalNodeGetParams(hNode, params_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExternalSemaphoresSignalNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExternalSemaphoresSignalNodeSetParams(CUgraphNode hNode, const CUDA_EXT_SEM_SIGNAL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExternalSemaphoresSignalNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExternalSemaphoresSignalNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddExternalSemaphoresWaitNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_EXT_SEM_WAIT_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddExternalSemaphoresWaitNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddExternalSemaphoresWaitNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExternalSemaphoresWaitNodeGetParams(CUgraphNode hNode, CUDA_EXT_SEM_WAIT_NODE_PARAMS* params_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExternalSemaphoresWaitNodeGetParams(hNode, params_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExternalSemaphoresWaitNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExternalSemaphoresWaitNodeSetParams(CUgraphNode hNode, const CUDA_EXT_SEM_WAIT_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExternalSemaphoresWaitNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExternalSemaphoresWaitNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddBatchMemOpNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_BATCH_MEM_OP_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddBatchMemOpNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddBatchMemOpNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphBatchMemOpNodeGetParams(CUgraphNode hNode, CUDA_BATCH_MEM_OP_NODE_PARAMS* nodeParams_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphBatchMemOpNodeGetParams(hNode, nodeParams_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphBatchMemOpNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphBatchMemOpNodeSetParams(CUgraphNode hNode, const CUDA_BATCH_MEM_OP_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphBatchMemOpNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphBatchMemOpNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecBatchMemOpNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_BATCH_MEM_OP_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecBatchMemOpNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecBatchMemOpNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddMemAllocNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUDA_MEM_ALLOC_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddMemAllocNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddMemAllocNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemAllocNodeGetParams(CUgraphNode hNode, CUDA_MEM_ALLOC_NODE_PARAMS* params_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemAllocNodeGetParams(hNode, params_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemAllocNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddMemFreeNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddMemFreeNode(phGraphNode, hGraph, dependencies, numDependencies, dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddMemFreeNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphMemFreeNodeGetParams(CUgraphNode hNode, CUdeviceptr* dptr_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphMemFreeNodeGetParams(hNode, dptr_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphMemFreeNodeGetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGraphMemTrim(CUdevice device) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGraphMemTrim(device); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGraphMemTrim %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetGraphMemAttribute(CUdevice device, CUgraphMem_attribute attr, void* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetGraphMemAttribute(device, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetGraphMemAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceSetGraphMemAttribute(CUdevice device, CUgraphMem_attribute attr, void* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceSetGraphMemAttribute(device, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceSetGraphMemAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphClone(CUgraph* phGraphClone, CUgraph originalGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphClone(phGraphClone, originalGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphClone %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeFindInClone(CUgraphNode* phNode, CUgraphNode hOriginalNode, CUgraph hClonedGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeFindInClone(phNode, hOriginalNode, hClonedGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeFindInClone %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetType(CUgraphNode hNode, CUgraphNodeType* type) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetType(hNode, type); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetType %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphGetNodes(CUgraph hGraph, CUgraphNode* nodes, size_t* numNodes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphGetNodes(hGraph, nodes, numNodes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphGetNodes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphGetRootNodes(CUgraph hGraph, CUgraphNode* rootNodes, size_t* numRootNodes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphGetRootNodes(hGraph, rootNodes, numRootNodes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphGetRootNodes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphGetEdges(CUgraph hGraph, CUgraphNode* from, CUgraphNode* to, size_t* numEdges) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphGetEdges(hGraph, from, to, numEdges); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphGetEdges %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphGetEdges_v2(CUgraph hGraph, CUgraphNode* from, CUgraphNode* to, CUgraphEdgeData* edgeData, size_t* numEdges) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphGetEdges_v2(hGraph, from, to, edgeData, numEdges); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphGetEdges_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetDependencies(CUgraphNode hNode, CUgraphNode* dependencies, size_t* numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetDependencies(hNode, dependencies, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetDependencies %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetDependencies_v2(CUgraphNode hNode, CUgraphNode* dependencies, CUgraphEdgeData* edgeData, size_t* numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetDependencies_v2(hNode, dependencies, edgeData, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetDependencies_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetDependentNodes(CUgraphNode hNode, CUgraphNode* dependentNodes, size_t* numDependentNodes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetDependentNodes(hNode, dependentNodes, numDependentNodes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetDependentNodes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetDependentNodes_v2(CUgraphNode hNode, CUgraphNode* dependentNodes, CUgraphEdgeData* edgeData, size_t* numDependentNodes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetDependentNodes_v2(hNode, dependentNodes, edgeData, numDependentNodes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetDependentNodes_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddDependencies(CUgraph hGraph, const CUgraphNode* from, const CUgraphNode* to, size_t numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddDependencies(hGraph, from, to, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddDependencies %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddDependencies_v2(CUgraph hGraph, const CUgraphNode* from, const CUgraphNode* to, const CUgraphEdgeData* edgeData, size_t numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddDependencies_v2(hGraph, from, to, edgeData, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddDependencies_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphRemoveDependencies(CUgraph hGraph, const CUgraphNode* from, const CUgraphNode* to, size_t numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphRemoveDependencies(hGraph, from, to, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphRemoveDependencies %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphRemoveDependencies_v2(CUgraph hGraph, const CUgraphNode* from, const CUgraphNode* to, const CUgraphEdgeData* edgeData, size_t numDependencies) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphRemoveDependencies_v2(hGraph, from, to, edgeData, numDependencies); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphRemoveDependencies_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphDestroyNode(CUgraphNode hNode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphDestroyNode(hNode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphDestroyNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphInstantiate(CUgraphExec* phGraphExec, CUgraph hGraph, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphInstantiate(phGraphExec, hGraph, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphInstantiate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphInstantiateWithParams(CUgraphExec* phGraphExec, CUgraph hGraph, CUDA_GRAPH_INSTANTIATE_PARAMS* instantiateParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphInstantiateWithParams(phGraphExec, hGraph, instantiateParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphInstantiateWithParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecGetFlags(CUgraphExec hGraphExec, cuuint64_t* flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecGetFlags(hGraphExec, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecGetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecKernelNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecKernelNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecKernelNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecMemcpyNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_MEMCPY3D* copyParams, CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecMemcpyNodeSetParams(hGraphExec, hNode, copyParams, ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecMemcpyNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecMemsetNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_MEMSET_NODE_PARAMS* memsetParams, CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecMemsetNodeSetParams(hGraphExec, hNode, memsetParams, ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecMemsetNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecHostNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_HOST_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecHostNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecHostNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecChildGraphNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, CUgraph childGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecChildGraphNodeSetParams(hGraphExec, hNode, childGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecChildGraphNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecEventRecordNodeSetEvent(CUgraphExec hGraphExec, CUgraphNode hNode, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecEventRecordNodeSetEvent(hGraphExec, hNode, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecEventRecordNodeSetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecEventWaitNodeSetEvent(CUgraphExec hGraphExec, CUgraphNode hNode, CUevent event) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecEventWaitNodeSetEvent(hGraphExec, hNode, event); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecEventWaitNodeSetEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecExternalSemaphoresSignalNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_EXT_SEM_SIGNAL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecExternalSemaphoresSignalNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecExternalSemaphoresSignalNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecExternalSemaphoresWaitNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_EXT_SEM_WAIT_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecExternalSemaphoresWaitNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecExternalSemaphoresWaitNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeSetEnabled(CUgraphExec hGraphExec, CUgraphNode hNode, unsigned int isEnabled) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeSetEnabled(hGraphExec, hNode, isEnabled); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeSetEnabled %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeGetEnabled(CUgraphExec hGraphExec, CUgraphNode hNode, unsigned int* isEnabled) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeGetEnabled(hGraphExec, hNode, isEnabled); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeGetEnabled %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphUpload(CUgraphExec hGraphExec, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphUpload(hGraphExec, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphUpload %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphLaunch(CUgraphExec hGraphExec, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphLaunch(hGraphExec, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphLaunch %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecDestroy(CUgraphExec hGraphExec) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecDestroy(hGraphExec); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphDestroy(CUgraph hGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphDestroy(hGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecUpdate(CUgraphExec hGraphExec, CUgraph hGraph, CUgraphExecUpdateResultInfo* resultInfo) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecUpdate(hGraphExec, hGraph, resultInfo); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecUpdate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeCopyAttributes(CUgraphNode dst, CUgraphNode src) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeCopyAttributes(dst, src); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeCopyAttributes %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeGetAttribute(CUgraphNode hNode, CUkernelNodeAttrID attr, CUkernelNodeAttrValue* value_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeGetAttribute(hNode, attr, value_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeSetAttribute(CUgraphNode hNode, CUkernelNodeAttrID attr, const CUkernelNodeAttrValue* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeSetAttribute(hNode, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphDebugDotPrint(CUgraph hGraph, const char* path, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphDebugDotPrint(hGraph, path, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphDebugDotPrint %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuUserObjectCreate(CUuserObject* object_out, void* ptr, CUhostFn destroy, unsigned int initialRefcount, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuUserObjectCreate(object_out, ptr, destroy, initialRefcount, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuUserObjectCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuUserObjectRetain(CUuserObject object, unsigned int count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuUserObjectRetain(object, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuUserObjectRetain %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuUserObjectRelease(CUuserObject object, unsigned int count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuUserObjectRelease(object, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuUserObjectRelease %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphRetainUserObject(CUgraph graph, CUuserObject object, unsigned int count, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphRetainUserObject(graph, object, count, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphRetainUserObject %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphReleaseUserObject(CUgraph graph, CUuserObject object, unsigned int count) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphReleaseUserObject(graph, object, count); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphReleaseUserObject %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddNode(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, CUgraphNodeParams* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddNode(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddNode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddNode_v2(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, const CUgraphEdgeData* dependencyData, size_t numDependencies, CUgraphNodeParams* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddNode_v2(phGraphNode, hGraph, dependencies, dependencyData, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddNode_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphNodeSetParams(CUgraphNode hNode, CUgraphNodeParams* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphNodeSetParams(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecNodeSetParams(CUgraphExec hGraphExec, CUgraphNode hNode, CUgraphNodeParams* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecNodeSetParams(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecNodeSetParams %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphConditionalHandleCreate(CUgraphConditionalHandle* pHandle_out, CUgraph hGraph, CUcontext ctx, unsigned int defaultLaunchValue, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphConditionalHandleCreate(pHandle_out, hGraph, ctx, defaultLaunchValue, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphConditionalHandleCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxActiveBlocksPerMultiprocessor(int* numBlocks, CUfunction func, int blockSize, size_t dynamicSMemSize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxActiveBlocksPerMultiprocessor(numBlocks, func, blockSize, dynamicSMemSize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxActiveBlocksPerMultiprocessor %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(int* numBlocks, CUfunction func, int blockSize, size_t dynamicSMemSize, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(numBlocks, func, blockSize, dynamicSMemSize, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxActiveBlocksPerMultiprocessorWithFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxPotentialBlockSize(int* minGridSize, int* blockSize, CUfunction func, CUoccupancyB2DSize blockSizeToDynamicSMemSize, size_t dynamicSMemSize, int blockSizeLimit) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxPotentialBlockSize(minGridSize, blockSize, func, blockSizeToDynamicSMemSize, dynamicSMemSize, blockSizeLimit); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxPotentialBlockSize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxPotentialBlockSizeWithFlags(int* minGridSize, int* blockSize, CUfunction func, CUoccupancyB2DSize blockSizeToDynamicSMemSize, size_t dynamicSMemSize, int blockSizeLimit, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxPotentialBlockSizeWithFlags(minGridSize, blockSize, func, blockSizeToDynamicSMemSize, dynamicSMemSize, blockSizeLimit, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxPotentialBlockSizeWithFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyAvailableDynamicSMemPerBlock(size_t* dynamicSmemSize, CUfunction func, int numBlocks, int blockSize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyAvailableDynamicSMemPerBlock(dynamicSmemSize, func, numBlocks, blockSize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyAvailableDynamicSMemPerBlock %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxPotentialClusterSize(int* clusterSize, CUfunction func, const CUlaunchConfig* config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxPotentialClusterSize(clusterSize, func, config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxPotentialClusterSize %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuOccupancyMaxActiveClusters(int* numClusters, CUfunction func, const CUlaunchConfig* config) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuOccupancyMaxActiveClusters(numClusters, func, config); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuOccupancyMaxActiveClusters %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetArray(CUtexref hTexRef, CUarray hArray, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetArray(hTexRef, hArray, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetMipmappedArray(CUtexref hTexRef, CUmipmappedArray hMipmappedArray, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetMipmappedArray(hTexRef, hMipmappedArray, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetMipmappedArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddress(size_t* ByteOffset, CUtexref hTexRef, CUdeviceptr dptr, size_t bytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddress(ByteOffset, hTexRef, dptr, bytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddress %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddress2D(CUtexref hTexRef, const CUDA_ARRAY_DESCRIPTOR* desc, CUdeviceptr dptr, size_t Pitch) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddress2D(hTexRef, desc, dptr, Pitch); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddress2D %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetFormat(CUtexref hTexRef, CUarray_format fmt, int NumPackedComponents) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetFormat(hTexRef, fmt, NumPackedComponents); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetFormat %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddressMode(CUtexref hTexRef, int dim, CUaddress_mode am) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddressMode(hTexRef, dim, am); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddressMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetFilterMode(CUtexref hTexRef, CUfilter_mode fm) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetFilterMode(hTexRef, fm); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetFilterMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetMipmapFilterMode(CUtexref hTexRef, CUfilter_mode fm) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetMipmapFilterMode(hTexRef, fm); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetMipmapFilterMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetMipmapLevelBias(CUtexref hTexRef, float bias) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetMipmapLevelBias(hTexRef, bias); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetMipmapLevelBias %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetMipmapLevelClamp(CUtexref hTexRef, float minMipmapLevelClamp, float maxMipmapLevelClamp) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetMipmapLevelClamp(hTexRef, minMipmapLevelClamp, maxMipmapLevelClamp); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetMipmapLevelClamp %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetMaxAnisotropy(CUtexref hTexRef, unsigned int maxAniso) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetMaxAnisotropy(hTexRef, maxAniso); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetMaxAnisotropy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetBorderColor(CUtexref hTexRef, float* pBorderColor) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetBorderColor(hTexRef, pBorderColor); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetBorderColor %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetFlags(CUtexref hTexRef, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetFlags(hTexRef, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetAddress(CUdeviceptr* pdptr, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetAddress(pdptr, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetAddress %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetArray(CUarray* phArray, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetArray(phArray, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetMipmappedArray(CUmipmappedArray* phMipmappedArray, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetMipmappedArray(phMipmappedArray, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetMipmappedArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetAddressMode(CUaddress_mode* pam, CUtexref hTexRef, int dim) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetAddressMode(pam, hTexRef, dim); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetAddressMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetFilterMode(CUfilter_mode* pfm, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetFilterMode(pfm, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetFilterMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetFormat(CUarray_format* pFormat, int* pNumChannels, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetFormat(pFormat, pNumChannels, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetFormat %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetMipmapFilterMode(CUfilter_mode* pfm, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetMipmapFilterMode(pfm, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetMipmapFilterMode %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetMipmapLevelBias(float* pbias, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetMipmapLevelBias(pbias, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetMipmapLevelBias %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetMipmapLevelClamp(float* pminMipmapLevelClamp, float* pmaxMipmapLevelClamp, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetMipmapLevelClamp(pminMipmapLevelClamp, pmaxMipmapLevelClamp, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetMipmapLevelClamp %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetMaxAnisotropy(int* pmaxAniso, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetMaxAnisotropy(pmaxAniso, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetMaxAnisotropy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetBorderColor(float* pBorderColor, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetBorderColor(pBorderColor, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetBorderColor %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetFlags(unsigned int* pFlags, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetFlags(pFlags, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefCreate(CUtexref* pTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefCreate(pTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefDestroy(CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefDestroy(hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSurfRefSetArray(CUsurfref hSurfRef, CUarray hArray, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSurfRefSetArray(hSurfRef, hArray, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSurfRefSetArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSurfRefGetArray(CUarray* phArray, CUsurfref hSurfRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSurfRefGetArray(phArray, hSurfRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSurfRefGetArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexObjectCreate(CUtexObject* pTexObject, const CUDA_RESOURCE_DESC* pResDesc, const CUDA_TEXTURE_DESC* pTexDesc, const CUDA_RESOURCE_VIEW_DESC* pResViewDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexObjectCreate(pTexObject, pResDesc, pTexDesc, pResViewDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexObjectCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexObjectDestroy(CUtexObject texObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexObjectDestroy(texObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexObjectDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexObjectGetResourceDesc(CUDA_RESOURCE_DESC* pResDesc, CUtexObject texObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexObjectGetResourceDesc(pResDesc, texObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexObjectGetResourceDesc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexObjectGetTextureDesc(CUDA_TEXTURE_DESC* pTexDesc, CUtexObject texObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexObjectGetTextureDesc(pTexDesc, texObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexObjectGetTextureDesc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexObjectGetResourceViewDesc(CUDA_RESOURCE_VIEW_DESC* pResViewDesc, CUtexObject texObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexObjectGetResourceViewDesc(pResViewDesc, texObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexObjectGetResourceViewDesc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSurfObjectCreate(CUsurfObject* pSurfObject, const CUDA_RESOURCE_DESC* pResDesc) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSurfObjectCreate(pSurfObject, pResDesc); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSurfObjectCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSurfObjectDestroy(CUsurfObject surfObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSurfObjectDestroy(surfObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSurfObjectDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSurfObjectGetResourceDesc(CUDA_RESOURCE_DESC* pResDesc, CUsurfObject surfObject) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSurfObjectGetResourceDesc(pResDesc, surfObject); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSurfObjectGetResourceDesc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTensorMapEncodeTiled(CUtensorMap* tensorMap, CUtensorMapDataType tensorDataType, cuuint32_t tensorRank, void* globalAddress, const cuuint64_t* globalDim, const cuuint64_t* globalStrides, const cuuint32_t* boxDim, const cuuint32_t* elementStrides, CUtensorMapInterleave interleave, CUtensorMapSwizzle swizzle, CUtensorMapL2promotion l2Promotion, CUtensorMapFloatOOBfill oobFill) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTensorMapEncodeTiled(tensorMap, tensorDataType, tensorRank, globalAddress, globalDim, globalStrides, boxDim, elementStrides, interleave, swizzle, l2Promotion, oobFill); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTensorMapEncodeTiled %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTensorMapEncodeIm2col(CUtensorMap* tensorMap, CUtensorMapDataType tensorDataType, cuuint32_t tensorRank, void* globalAddress, const cuuint64_t* globalDim, const cuuint64_t* globalStrides, const int* pixelBoxLowerCorner, const int* pixelBoxUpperCorner, cuuint32_t channelsPerPixel, cuuint32_t pixelsPerColumn, const cuuint32_t* elementStrides, CUtensorMapInterleave interleave, CUtensorMapSwizzle swizzle, CUtensorMapL2promotion l2Promotion, CUtensorMapFloatOOBfill oobFill) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTensorMapEncodeIm2col(tensorMap, tensorDataType, tensorRank, globalAddress, globalDim, globalStrides, pixelBoxLowerCorner, pixelBoxUpperCorner, channelsPerPixel, pixelsPerColumn, elementStrides, interleave, swizzle, l2Promotion, oobFill); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTensorMapEncodeIm2col %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTensorMapReplaceAddress(CUtensorMap* tensorMap, void* globalAddress) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTensorMapReplaceAddress(tensorMap, globalAddress); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTensorMapReplaceAddress %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceCanAccessPeer(int* canAccessPeer, CUdevice dev, CUdevice peerDev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceCanAccessPeer(canAccessPeer, dev, peerDev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceCanAccessPeer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxEnablePeerAccess(CUcontext peerContext, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxEnablePeerAccess(peerContext, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxEnablePeerAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxDisablePeerAccess(CUcontext peerContext) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxDisablePeerAccess(peerContext); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxDisablePeerAccess %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetP2PAttribute(int* value, CUdevice_P2PAttribute attrib, CUdevice srcDevice, CUdevice dstDevice) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetP2PAttribute(value, attrib, srcDevice, dstDevice); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetP2PAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsUnregisterResource(CUgraphicsResource resource) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsUnregisterResource(resource); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsUnregisterResource %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsSubResourceGetMappedArray(CUarray* pArray, CUgraphicsResource resource, unsigned int arrayIndex, unsigned int mipLevel) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsSubResourceGetMappedArray(pArray, resource, arrayIndex, mipLevel); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsSubResourceGetMappedArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsResourceGetMappedMipmappedArray(CUmipmappedArray* pMipmappedArray, CUgraphicsResource resource) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsResourceGetMappedMipmappedArray(pMipmappedArray, resource); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsResourceGetMappedMipmappedArray %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsResourceGetMappedPointer(CUdeviceptr* pDevPtr, size_t* pSize, CUgraphicsResource resource) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsResourceGetMappedPointer(pDevPtr, pSize, resource); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsResourceGetMappedPointer %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsResourceSetMapFlags(CUgraphicsResource resource, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsResourceSetMapFlags(resource, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsResourceSetMapFlags %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsMapResources(unsigned int count, CUgraphicsResource* resources, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsMapResources(count, resources, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsMapResources %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsUnmapResources(unsigned int count, CUgraphicsResource* resources, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsUnmapResources(count, resources, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsUnmapResources %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCoredumpGetAttribute(CUcoredumpSettings attrib, void* value, size_t* size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCoredumpGetAttribute(attrib, value, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCoredumpGetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCoredumpGetAttributeGlobal(CUcoredumpSettings attrib, void* value, size_t* size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCoredumpGetAttributeGlobal(attrib, value, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCoredumpGetAttributeGlobal %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCoredumpSetAttribute(CUcoredumpSettings attrib, void* value, size_t* size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCoredumpSetAttribute(attrib, value, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCoredumpSetAttribute %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCoredumpSetAttributeGlobal(CUcoredumpSettings attrib, void* value, size_t* size) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCoredumpSetAttributeGlobal(attrib, value, size); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCoredumpSetAttributeGlobal %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGetExportTable(const void** ppExportTable, const CUuuid* pExportTableId) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGetExportTable(ppExportTable, pExportTableId); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGetExportTable %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGreenCtxCreate(CUgreenCtx* phCtx, CUdevResourceDesc desc, CUdevice dev, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGreenCtxCreate(phCtx, desc, dev, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGreenCtxCreate %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGreenCtxDestroy(CUgreenCtx hCtx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGreenCtxDestroy(hCtx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGreenCtxDestroy %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxFromGreenCtx(CUcontext* pContext, CUgreenCtx hCtx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxFromGreenCtx(pContext, hCtx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxFromGreenCtx %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceGetDevResource(CUdevice device, CUdevResource* resource, CUdevResourceType type) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceGetDevResource(device, resource, type); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceGetDevResource %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxGetDevResource(CUcontext hCtx, CUdevResource* resource, CUdevResourceType type) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxGetDevResource(hCtx, resource, type); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxGetDevResource %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGreenCtxGetDevResource(CUgreenCtx hCtx, CUdevResource* resource, CUdevResourceType type) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGreenCtxGetDevResource(hCtx, resource, type); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGreenCtxGetDevResource %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevSmResourceSplitByCount(CUdevResource* result, unsigned int* nbGroups, const CUdevResource* input, CUdevResource* remaining, unsigned int useFlags, unsigned int minCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevSmResourceSplitByCount(result, nbGroups, input, remaining, useFlags, minCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevSmResourceSplitByCount %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevResourceGenerateDesc(CUdevResourceDesc* phDesc, CUdevResource* resources, unsigned int nbResources) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevResourceGenerateDesc(phDesc, resources, nbResources); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevResourceGenerateDesc %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGreenCtxRecordEvent(CUgreenCtx hCtx, CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGreenCtxRecordEvent(hCtx, hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGreenCtxRecordEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGreenCtxWaitEvent(CUgreenCtx hCtx, CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGreenCtxWaitEvent(hCtx, hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGreenCtxWaitEvent %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetGreenCtx(CUstream hStream, CUgreenCtx* phCtx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetGreenCtx(hStream, phCtx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetGreenCtx %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArray3DCreate_v2(CUarray* pHandle, const CUDA_ARRAY3D_DESCRIPTOR* pAllocateArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArray3DCreate_v2(pHandle, pAllocateArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArray3DCreate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArray3DGetDescriptor_v2(CUDA_ARRAY3D_DESCRIPTOR* pArrayDescriptor, CUarray hArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArray3DGetDescriptor_v2(pArrayDescriptor, hArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArray3DGetDescriptor_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayCreate_v2(CUarray* pHandle, const CUDA_ARRAY_DESCRIPTOR* pAllocateArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayCreate_v2(pHandle, pAllocateArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayCreate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuArrayGetDescriptor_v2(CUDA_ARRAY_DESCRIPTOR* pArrayDescriptor, CUarray hArray) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuArrayGetDescriptor_v2(pArrayDescriptor, hArray); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuArrayGetDescriptor_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxCreate_v2(CUcontext* pctx, unsigned int flags, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxCreate_v2(pctx, flags, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxCreate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxDestroy_v2(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxDestroy_v2(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxDestroy_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxPopCurrent_v2(CUcontext* pctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxPopCurrent_v2(pctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxPopCurrent_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuCtxPushCurrent_v2(CUcontext ctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuCtxPushCurrent_v2(ctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuCtxPushCurrent_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxRelease_v2(CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxRelease_v2(dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxRelease_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxReset_v2(CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxReset_v2(dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxReset_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDevicePrimaryCtxSetFlags_v2(CUdevice dev, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDevicePrimaryCtxSetFlags_v2(dev, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDevicePrimaryCtxSetFlags_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuDeviceTotalMem_v2(size_t* bytes, CUdevice dev) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuDeviceTotalMem_v2(bytes, dev); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuDeviceTotalMem_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventDestroy_v2(CUevent hEvent) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventDestroy_v2(hEvent); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventDestroy_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventRecord_ptsz(CUevent hEvent, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventRecord_ptsz(hEvent, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventRecord_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuEventRecordWithFlags_ptsz(CUevent hEvent, CUstream hStream, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuEventRecordWithFlags_ptsz(hEvent, hStream, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuEventRecordWithFlags_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphAddKernelNode_v2(CUgraphNode* phGraphNode, CUgraph hGraph, const CUgraphNode* dependencies, size_t numDependencies, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphAddKernelNode_v2(phGraphNode, hGraph, dependencies, numDependencies, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphAddKernelNode_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecKernelNodeSetParams_v2(CUgraphExec hGraphExec, CUgraphNode hNode, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecKernelNodeSetParams_v2(hGraphExec, hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecKernelNodeSetParams_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphExecUpdate_v2(CUgraphExec hGraphExec, CUgraph hGraph, CUgraphExecUpdateResultInfo* resultInfo) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphExecUpdate_v2(hGraphExec, hGraph, resultInfo); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphExecUpdate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsMapResources_ptsz(unsigned int count, CUgraphicsResource* resources, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsMapResources_ptsz(count, resources, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsMapResources_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsResourceGetMappedPointer_v2(CUdeviceptr* pDevPtr, size_t* pSize, CUgraphicsResource resource) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsResourceGetMappedPointer_v2(pDevPtr, pSize, resource); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsResourceGetMappedPointer_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsResourceSetMapFlags_v2(CUgraphicsResource resource, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsResourceSetMapFlags_v2(resource, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsResourceSetMapFlags_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphicsUnmapResources_ptsz(unsigned int count, CUgraphicsResource* resources, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphicsUnmapResources_ptsz(count, resources, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphicsUnmapResources_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphInstantiate_v2(CUgraphExec* phGraphExec, CUgraph hGraph, unsigned long long flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphInstantiate_v2(phGraphExec, hGraph, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphInstantiate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphInstantiateWithParams_ptsz(CUgraphExec* phGraphExec, CUgraph hGraph, CUDA_GRAPH_INSTANTIATE_PARAMS* instantiateParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphInstantiateWithParams_ptsz(phGraphExec, hGraph, instantiateParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphInstantiateWithParams_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeGetParams_v2(CUgraphNode hNode, CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeGetParams_v2(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeGetParams_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphKernelNodeSetParams_v2(CUgraphNode hNode, const CUDA_KERNEL_NODE_PARAMS* nodeParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphKernelNodeSetParams_v2(hNode, nodeParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphKernelNodeSetParams_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphLaunch_ptsz(CUgraphExec hGraphExec, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphLaunch_ptsz(hGraphExec, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphLaunch_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuGraphUpload_ptsz(CUgraphExec hGraphExec, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuGraphUpload_ptsz(hGraphExec, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuGraphUpload_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuIpcOpenMemHandle_v2(CUdeviceptr* pdptr, CUipcMemHandle handle, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuIpcOpenMemHandle_v2(pdptr, handle, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuIpcOpenMemHandle_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchCooperativeKernel_ptsz(CUfunction f, unsigned int gridDimX, unsigned int gridDimY, unsigned int gridDimZ, unsigned int blockDimX, unsigned int blockDimY, unsigned int blockDimZ, unsigned int sharedMemBytes, CUstream hStream, void** kernelParams) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchCooperativeKernel_ptsz(f, gridDimX, gridDimY, gridDimZ, blockDimX, blockDimY, blockDimZ, sharedMemBytes, hStream, kernelParams); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchCooperativeKernel_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchHostFunc_ptsz(CUstream hStream, CUhostFn fn, void* userData) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchHostFunc_ptsz(hStream, fn, userData); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchHostFunc_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchKernelEx_ptsz(const CUlaunchConfig* config, CUfunction f, void** kernelParams, void** extra) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchKernelEx_ptsz(config, f, kernelParams, extra); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchKernelEx_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLaunchKernel_ptsz(CUfunction f, unsigned int gridDimX, unsigned int gridDimY, unsigned int gridDimZ, unsigned int blockDimX, unsigned int blockDimY, unsigned int blockDimZ, unsigned int sharedMemBytes, CUstream hStream, void** kernelParams, void** extra) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLaunchKernel_ptsz(f, gridDimX, gridDimY, gridDimZ, blockDimX, blockDimY, blockDimZ, sharedMemBytes, hStream, kernelParams, extra); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLaunchKernel_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkAddData_v2(CUlinkState state, CUjitInputType type, void* data, size_t size, const char* name, unsigned int numOptions, CUjit_option* options, void** optionValues) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkAddData_v2(state, type, data, size, name, numOptions, options, optionValues); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkAddData_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkAddFile_v2(CUlinkState state, CUjitInputType type, const char* path, unsigned int numOptions, CUjit_option* options, void** optionValues) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkAddFile_v2(state, type, path, numOptions, options, optionValues); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkAddFile_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuLinkCreate_v2(unsigned int numOptions, CUjit_option* options, void** optionValues, CUlinkState* stateOut) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuLinkCreate_v2(numOptions, options, optionValues, stateOut); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuLinkCreate_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocAsync_ptsz(CUdeviceptr* dptr, size_t bytesize, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocAsync_ptsz(dptr, bytesize, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocFromPoolAsync_ptsz(CUdeviceptr* dptr, size_t bytesize, CUmemoryPool pool, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocFromPoolAsync_ptsz(dptr, bytesize, pool, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocFromPoolAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocHost_v2(void** pp, size_t bytesize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocHost_v2(pp, bytesize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocHost_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAllocPitch_v2(CUdeviceptr* dptr, size_t* pPitch, size_t WidthInBytes, size_t Height, unsigned int ElementSizeBytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAllocPitch_v2(dptr, pPitch, WidthInBytes, Height, ElementSizeBytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAllocPitch_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemAlloc_v2(CUdeviceptr* dptr, size_t bytesize) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemAlloc_v2(dptr, bytesize); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemAlloc_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DAsync_v2(const CUDA_MEMCPY2D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DAsync_v2(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DAsync_v2_ptsz(const CUDA_MEMCPY2D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DAsync_v2_ptsz(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DUnaligned_v2(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DUnaligned_v2(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DUnaligned_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2DUnaligned_v2_ptds(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2DUnaligned_v2_ptds(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2DUnaligned_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2D_v2(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2D_v2(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2D_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy2D_v2_ptds(const CUDA_MEMCPY2D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy2D_v2_ptds(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy2D_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DAsync_v2(const CUDA_MEMCPY3D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DAsync_v2(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DAsync_v2_ptsz(const CUDA_MEMCPY3D* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DAsync_v2_ptsz(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DPeerAsync_ptsz(const CUDA_MEMCPY3D_PEER* pCopy, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DPeerAsync_ptsz(pCopy, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DPeerAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3DPeer_ptds(const CUDA_MEMCPY3D_PEER* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3DPeer_ptds(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3DPeer_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3D_v2(const CUDA_MEMCPY3D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3D_v2(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3D_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy3D_v2_ptds(const CUDA_MEMCPY3D* pCopy) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy3D_v2_ptds(pCopy); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy3D_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAsync_ptsz(CUdeviceptr dst, CUdeviceptr src, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAsync_ptsz(dst, src, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoA_v2(CUarray dstArray, size_t dstOffset, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoA_v2(dstArray, dstOffset, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoA_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoA_v2_ptds(CUarray dstArray, size_t dstOffset, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoA_v2_ptds(dstArray, dstOffset, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoA_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoD_v2(CUdeviceptr dstDevice, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoD_v2(dstDevice, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoD_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoD_v2_ptds(CUdeviceptr dstDevice, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoD_v2_ptds(dstDevice, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoD_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoHAsync_v2(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoHAsync_v2(dstHost, srcArray, srcOffset, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoHAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoHAsync_v2_ptsz(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoHAsync_v2_ptsz(dstHost, srcArray, srcOffset, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoHAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoH_v2(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoH_v2(dstHost, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoH_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyAtoH_v2_ptds(void* dstHost, CUarray srcArray, size_t srcOffset, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyAtoH_v2_ptds(dstHost, srcArray, srcOffset, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyAtoH_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoA_v2(CUarray dstArray, size_t dstOffset, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoA_v2(dstArray, dstOffset, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoA_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoA_v2_ptds(CUarray dstArray, size_t dstOffset, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoA_v2_ptds(dstArray, dstOffset, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoA_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoDAsync_v2(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoDAsync_v2(dstDevice, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoDAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoDAsync_v2_ptsz(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoDAsync_v2_ptsz(dstDevice, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoDAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoD_v2(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoD_v2(dstDevice, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoD_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoD_v2_ptds(CUdeviceptr dstDevice, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoD_v2_ptds(dstDevice, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoD_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoHAsync_v2(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoHAsync_v2(dstHost, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoHAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoHAsync_v2_ptsz(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoHAsync_v2_ptsz(dstHost, srcDevice, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoHAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoH_v2(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoH_v2(dstHost, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoH_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyDtoH_v2_ptds(void* dstHost, CUdeviceptr srcDevice, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyDtoH_v2_ptds(dstHost, srcDevice, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyDtoH_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoAAsync_v2(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoAAsync_v2(dstArray, dstOffset, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoAAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoAAsync_v2_ptsz(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoAAsync_v2_ptsz(dstArray, dstOffset, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoAAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoA_v2(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoA_v2(dstArray, dstOffset, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoA_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoA_v2_ptds(CUarray dstArray, size_t dstOffset, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoA_v2_ptds(dstArray, dstOffset, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoA_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoDAsync_v2(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoDAsync_v2(dstDevice, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoDAsync_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoDAsync_v2_ptsz(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoDAsync_v2_ptsz(dstDevice, srcHost, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoDAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoD_v2(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoD_v2(dstDevice, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoD_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyHtoD_v2_ptds(CUdeviceptr dstDevice, const void* srcHost, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyHtoD_v2_ptds(dstDevice, srcHost, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyHtoD_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyPeerAsync_ptsz(CUdeviceptr dstDevice, CUcontext dstContext, CUdeviceptr srcDevice, CUcontext srcContext, size_t ByteCount, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyPeerAsync_ptsz(dstDevice, dstContext, srcDevice, srcContext, ByteCount, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyPeerAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpyPeer_ptds(CUdeviceptr dstDevice, CUcontext dstContext, CUdeviceptr srcDevice, CUcontext srcContext, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpyPeer_ptds(dstDevice, dstContext, srcDevice, srcContext, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpyPeer_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemcpy_ptds(CUdeviceptr dst, CUdeviceptr src, size_t ByteCount) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemcpy_ptds(dst, src, ByteCount); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemcpy_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemFreeAsync_ptsz(CUdeviceptr dptr, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemFreeAsync_ptsz(dptr, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemFreeAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemFree_v2(CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemFree_v2(dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemFree_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetAddressRange_v2(CUdeviceptr* pbase, size_t* psize, CUdeviceptr dptr) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetAddressRange_v2(pbase, psize, dptr); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetAddressRange_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemGetInfo_v2(size_t* free, size_t* total) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemGetInfo_v2(free, total); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemGetInfo_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostGetDevicePointer_v2(CUdeviceptr* pdptr, void* p, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostGetDevicePointer_v2(pdptr, p, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostGetDevicePointer_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemHostRegister_v2(void* p, size_t bytesize, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemHostRegister_v2(p, bytesize, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemHostRegister_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemMapArrayAsync_ptsz(CUarrayMapInfo* mapInfoList, unsigned int count, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemMapArrayAsync_ptsz(mapInfoList, count, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemMapArrayAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPrefetchAsync_ptsz(CUdeviceptr devPtr, size_t count, CUdevice dstDevice, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPrefetchAsync_ptsz(devPtr, count, dstDevice, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPrefetchAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemPrefetchAsync_v2_ptsz(CUdeviceptr devPtr, size_t count, CUdevice dstDevice, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemPrefetchAsync_v2_ptsz(devPtr, count, dstDevice, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemPrefetchAsync_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD16Async_ptsz(CUdeviceptr dstDevice, unsigned short us, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD16Async_ptsz(dstDevice, us, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD16Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD16_v2(CUdeviceptr dstDevice, unsigned short us, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD16_v2(dstDevice, us, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD16_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD16_v2_ptds(CUdeviceptr dstDevice, unsigned short us, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD16_v2_ptds(dstDevice, us, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD16_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D16Async_ptsz(CUdeviceptr dstDevice, size_t dstPitch, unsigned short us, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D16Async_ptsz(dstDevice, dstPitch, us, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D16Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D16_v2(CUdeviceptr dstDevice, size_t dstPitch, unsigned short us, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D16_v2(dstDevice, dstPitch, us, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D16_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D16_v2_ptds(CUdeviceptr dstDevice, size_t dstPitch, unsigned short us, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D16_v2_ptds(dstDevice, dstPitch, us, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D16_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D32Async_ptsz(CUdeviceptr dstDevice, size_t dstPitch, unsigned int ui, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D32Async_ptsz(dstDevice, dstPitch, ui, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D32Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D32_v2(CUdeviceptr dstDevice, size_t dstPitch, unsigned int ui, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D32_v2(dstDevice, dstPitch, ui, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D32_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D32_v2_ptds(CUdeviceptr dstDevice, size_t dstPitch, unsigned int ui, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D32_v2_ptds(dstDevice, dstPitch, ui, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D32_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D8Async_ptsz(CUdeviceptr dstDevice, size_t dstPitch, unsigned char uc, size_t Width, size_t Height, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D8Async_ptsz(dstDevice, dstPitch, uc, Width, Height, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D8Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D8_v2(CUdeviceptr dstDevice, size_t dstPitch, unsigned char uc, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D8_v2(dstDevice, dstPitch, uc, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D8_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD2D8_v2_ptds(CUdeviceptr dstDevice, size_t dstPitch, unsigned char uc, size_t Width, size_t Height) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD2D8_v2_ptds(dstDevice, dstPitch, uc, Width, Height); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD2D8_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD32Async_ptsz(CUdeviceptr dstDevice, unsigned int ui, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD32Async_ptsz(dstDevice, ui, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD32Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD32_v2(CUdeviceptr dstDevice, unsigned int ui, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD32_v2(dstDevice, ui, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD32_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD32_v2_ptds(CUdeviceptr dstDevice, unsigned int ui, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD32_v2_ptds(dstDevice, ui, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD32_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD8Async_ptsz(CUdeviceptr dstDevice, unsigned char uc, size_t N, CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD8Async_ptsz(dstDevice, uc, N, hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD8Async_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD8_v2(CUdeviceptr dstDevice, unsigned char uc, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD8_v2(dstDevice, uc, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD8_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuMemsetD8_v2_ptds(CUdeviceptr dstDevice, unsigned char uc, size_t N) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuMemsetD8_v2_ptds(dstDevice, uc, N); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuMemsetD8_v2_ptds %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuModuleGetGlobal_v2(CUdeviceptr* dptr, size_t* bytes, CUmodule hmod, const char* name) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuModuleGetGlobal_v2(dptr, bytes, hmod, name); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuModuleGetGlobal_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuSignalExternalSemaphoresAsync_ptsz(const CUexternalSemaphore* extSemArray, const CUDA_EXTERNAL_SEMAPHORE_SIGNAL_PARAMS* paramsArray, unsigned int numExtSems, CUstream stream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuSignalExternalSemaphoresAsync_ptsz(extSemArray, paramsArray, numExtSems, stream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuSignalExternalSemaphoresAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamAddCallback_ptsz(CUstream hStream, CUstreamCallback callback, void* userData, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamAddCallback_ptsz(hStream, callback, userData, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamAddCallback_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamAttachMemAsync_ptsz(CUstream hStream, CUdeviceptr dptr, size_t length, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamAttachMemAsync_ptsz(hStream, dptr, length, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamAttachMemAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBatchMemOp_ptsz(CUstream stream, unsigned int count, CUstreamBatchMemOpParams* paramArray, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBatchMemOp_ptsz(stream, count, paramArray, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBatchMemOp_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBatchMemOp_v2(CUstream stream, unsigned int count, CUstreamBatchMemOpParams* paramArray, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBatchMemOp_v2(stream, count, paramArray, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBatchMemOp_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBatchMemOp_v2_ptsz(CUstream stream, unsigned int count, CUstreamBatchMemOpParams* paramArray, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBatchMemOp_v2_ptsz(stream, count, paramArray, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBatchMemOp_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCapture_ptsz(CUstream hStream, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCapture_ptsz(hStream, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCapture_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCaptureToGraph_ptsz(CUstream hStream, CUgraph hGraph, const CUgraphNode* dependencies, const CUgraphEdgeData* dependencyData, size_t numDependencies, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCaptureToGraph_ptsz(hStream, hGraph, dependencies, dependencyData, numDependencies, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCaptureToGraph_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCapture_v2(CUstream hStream, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCapture_v2(hStream, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCapture_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamBeginCapture_v2_ptsz(CUstream hStream, CUstreamCaptureMode mode) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamBeginCapture_v2_ptsz(hStream, mode); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamBeginCapture_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamCopyAttributes_ptsz(CUstream dst, CUstream src) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamCopyAttributes_ptsz(dst, src); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamCopyAttributes_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamDestroy_v2(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamDestroy_v2(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamDestroy_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamEndCapture_ptsz(CUstream hStream, CUgraph* phGraph) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamEndCapture_ptsz(hStream, phGraph); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamEndCapture_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetAttribute_ptsz(CUstream hStream, CUstreamAttrID attr, CUstreamAttrValue* value_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetAttribute_ptsz(hStream, attr, value_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetAttribute_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo_ptsz(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo_ptsz(hStream, captureStatus_out, id_out, graph_out, dependencies_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo_v2(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo_v2(hStream, captureStatus_out, id_out, graph_out, dependencies_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo_v2_ptsz(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo_v2_ptsz(hStream, captureStatus_out, id_out, graph_out, dependencies_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCaptureInfo_v3_ptsz(CUstream hStream, CUstreamCaptureStatus* captureStatus_out, cuuint64_t* id_out, CUgraph* graph_out, const CUgraphNode** dependencies_out, size_t* numDependencies_out) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCaptureInfo_v3_ptsz(hStream, captureStatus_out, id_out, graph_out, dependencies_out, numDependencies_out); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCaptureInfo_v3_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetCtx_ptsz(CUstream hStream, CUcontext* pctx) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetCtx_ptsz(hStream, pctx); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetCtx_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetFlags_ptsz(CUstream hStream, unsigned int* flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetFlags_ptsz(hStream, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetFlags_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetId_ptsz(CUstream hStream, unsigned long long* streamId) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetId_ptsz(hStream, streamId); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetId_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamGetPriority_ptsz(CUstream hStream, int* priority) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamGetPriority_ptsz(hStream, priority); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamGetPriority_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamIsCapturing_ptsz(CUstream hStream, CUstreamCaptureStatus* captureStatus) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamIsCapturing_ptsz(hStream, captureStatus); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamIsCapturing_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamQuery_ptsz(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamQuery_ptsz(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamQuery_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamSetAttribute_ptsz(CUstream hStream, CUstreamAttrID attr, const CUstreamAttrValue* value) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamSetAttribute_ptsz(hStream, attr, value); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamSetAttribute_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamSynchronize_ptsz(CUstream hStream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamSynchronize_ptsz(hStream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamSynchronize_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamUpdateCaptureDependencies_ptsz(CUstream hStream, CUgraphNode* dependencies, size_t numDependencies, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamUpdateCaptureDependencies_ptsz(hStream, dependencies, numDependencies, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamUpdateCaptureDependencies_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamUpdateCaptureDependencies_v2_ptsz(CUstream hStream, CUgraphNode* dependencies, size_t numDependencies, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamUpdateCaptureDependencies_v2_ptsz(hStream, dependencies, numDependencies, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamUpdateCaptureDependencies_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitEvent_ptsz(CUstream hStream, CUevent hEvent, unsigned int Flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitEvent_ptsz(hStream, hEvent, Flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitEvent_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue32_ptsz(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue32_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue32_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue32_v2(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue32_v2(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue32_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue32_v2_ptsz(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue32_v2_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue32_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue64_ptsz(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue64_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue64_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue64_v2(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue64_v2(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue64_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWaitValue64_v2_ptsz(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWaitValue64_v2_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWaitValue64_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue32_ptsz(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue32_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue32_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue32_v2(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue32_v2(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue32_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue32_v2_ptsz(CUstream stream, CUdeviceptr addr, cuuint32_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue32_v2_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue32_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue64_ptsz(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue64_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue64_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue64_v2(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue64_v2(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue64_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuStreamWriteValue64_v2_ptsz(CUstream stream, CUdeviceptr addr, cuuint64_t value, unsigned int flags) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuStreamWriteValue64_v2_ptsz(stream, addr, value, flags); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuStreamWriteValue64_v2_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefGetAddress_v2(CUdeviceptr* pdptr, CUtexref hTexRef) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefGetAddress_v2(pdptr, hTexRef); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefGetAddress_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddress2D_v2(CUtexref hTexRef, const CUDA_ARRAY_DESCRIPTOR* desc, CUdeviceptr dptr, size_t Pitch) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddress2D_v2(hTexRef, desc, dptr, Pitch); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddress2D_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddress2D_v3(CUtexref hTexRef, const CUDA_ARRAY_DESCRIPTOR* desc, CUdeviceptr dptr, size_t Pitch) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddress2D_v3(hTexRef, desc, dptr, Pitch); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddress2D_v3 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuTexRefSetAddress_v2(size_t* ByteOffset, CUtexref hTexRef, CUdeviceptr dptr, size_t bytes) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuTexRefSetAddress_v2(ByteOffset, hTexRef, dptr, bytes); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuTexRefSetAddress_v2 %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}

CUresult cuWaitExternalSemaphoresAsync_ptsz(const CUexternalSemaphore* extSemArray, const CUDA_EXTERNAL_SEMAPHORE_WAIT_PARAMS* paramsArray, unsigned int numExtSems, CUstream stream) {
    if (shared_lib == NULL)  { ld_init(); }
    CUresult err = real_cuWaitExternalSemaphoresAsync_ptsz(extSemArray, paramsArray, numExtSems, stream); // call the real
    if (VERBOSE)  { 
        fprintf(event_log, "[info] cuWaitExternalSemaphoresAsync_ptsz %d\n", err); 
        fflush(event_log); // block until output written for debugging
    }
    return err;
}
