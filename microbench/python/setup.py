from setuptools import setup, find_packages

setup(
    name='mycutlass',
    version='0.1',
    packages=find_packages(),
    description='Python interface for mycutlass library',
    install_requires=[
        # 'torch', # Optional, but recommended for usage
    ],
)
